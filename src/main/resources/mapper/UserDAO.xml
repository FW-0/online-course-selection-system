<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.gdpu.mapper.UserDAO">
    <resultMap id="userMap" type="user">
        <id property="studentName" column="user_name"></id>
        <result property="username" column="user_account"></result>
        <result property="password" column="user_password"></result>
        <result property="object" column="user_object"></result>
        <result property="role" column="role"></result>
    </resultMap>
    
    <select id="login" parameterType="string" resultMap="userMap">
        select user_name, user_account, user_password, user_object, role from user where user_account = #{username}
    </select>
    
    <select id="selectAll" resultMap="userMap">
        select user_name, user_account, user_password, user_object, role  from user
    </select>
    <select id="getUserByUsername" parameterType="string" resultMap="userMap">
        select user_name, user_account, user_password, user_object, role
        from user
        where user_account = #{username}
    </select>
    <update id="updatePassword" parameterType="user">
        update user
        set user_password = #{password}
        where user_account = #{username}
    </update>
</mapper>